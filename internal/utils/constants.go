package utils

// Session cookie name.
const SessionName = "ssh_manager_session"

// Keys within a session.
const (
	IsAuthenticated = "authenticated"
	UsernameKey     = "username"
	UserIDKey       = "user_id"
)
